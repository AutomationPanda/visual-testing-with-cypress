describe('Trello', () => {

  beforeEach(() => {
    // Reset app data
  })

  it('can create a new board', () => {
    // Load the home page
    // Verify the home page loaded
    // Create a new board
    // Verify the new board is created
  })
})